# Python
__pycache__/
*.py[cod]
venv/
.venv/

# Environment variables - NEVER upload these to GitHub
# These contain secret API keys — set them in Render/Vercel dashboard instead
.env
*.env

# Data files - too large for GitHub (500K+ ball-by-ball CSV files)
data/raw/

# ML model files — INCLUDED in git (only ~1.6MB, needed by Render)
# *.pkl   ← intentionally NOT ignored so Render can use the trained models
# *.joblib

# Node / React
node_modules/
dist/
build/

# Mac junk
.DS_Store

# Jupyter checkpoints
.ipynb_checkpoints/
